#include <stdio.h>

    int main()
    {
        float raio = 1;
        float const pi = 3.14;
        float area = 1*1*pi;

        printf("A area de um circulo de raio %f \n", raio);
        printf("e igual a %.2f", area);

    return 0;
    }
